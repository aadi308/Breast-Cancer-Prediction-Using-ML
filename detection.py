# -*- coding: utf-8 -*-
"""detection.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1AtpiLz875pk-LM51X6h2-EbrLeGwaTb_

Source:
Comes from the University of Wisconsin Hospital,
Available directly through scikit-learn

The dataset is designed to help build models that can predict whether a breast tumor is malignant (cancerous) or benign (non-cancerous) based on features extracted from breast tissue sample images.
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.datasets import load_breast_cancer
from sklearn.model_selection import train_test_split, GridSearchCV
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import confusion_matrix, classification_report, accuracy_score, f1_score, precision_score, recall_score
from sklearn.svm import SVC
from sklearn.neighbors import KNeighborsClassifier
from sklearn.naive_bayes import GaussianNB
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import AdaBoostClassifier, RandomForestClassifier
from sklearn.linear_model import LogisticRegression
from xgboost import XGBClassifier

#loading the dataset
cancer_dataset = load_breast_cancer()

type(cancer_dataset) #Source

cancer_dataset.keys()
#Shows all the available components of
#the breast cancer dataset, such as data, target, feature names, and description.

cancer_dataset['data'] #Displays the feature matrix containing all numerical measurements for every sample in the dataset

cancer_dataset['target'] #Number of samples. 569 total patient records

cancer_dataset['target_names'] #Target 2 Classes (Cancer & non-cancer)

print(cancer_dataset['DESCR'])

#Number of features, 30 numerical features
# These come from digitized images.
#characteristics like (shape, texture, symmetry, etc.)

print(cancer_dataset['feature_names']) #Feature Categories, Each cell nucleus has 10 basic measurements
#But for each of the 10 features, three types are recorded (Mean, Worst, Standerd Error)

print(cancer_dataset['filename']) #Example Feature Names.
#These help the model learn the differences between malignant and benign tumors

cancer_df = pd.DataFrame(np.c_[cancer_dataset['data'],cancer_dataset['target']],  #dependend(target) and independent(features) variables seperations
             columns = np.append(cancer_dataset['feature_names'], ['target']))
#Take the data and the target, combine them into one table, and give all the columns proper names.

cancer_df.head(6)

cancer_df.tail(6)

cancer_df.info() #Displays the last 6 records of the dataset

cancer_df.describe() #Generates summary statistics for all numerical features in the dataset

sns.pairplot(cancer_df, hue = 'target') #dataspreaded
#Creates scatterplot pairs of all features, colored by the target class,
#to visualize how the data is spread and how classes separate

sns.pairplot(cancer_df, hue = 'target',
             vars = ['mean radius', 'mean texture', 'mean perimeter', 'mean area', 'mean smoothness'] )
#Creates pairwise scatterplots for selected features, colored by the target class,
#to visualize their relationships and class separation.

sns.countplot(cancer_df['target'])
#Plots the number of samples in each target class
 #(malignant vs. benign) to show class distribution.

plt.figure(figsize = (20,8))
sns.countplot(cancer_df['mean radius'])
#Creates a large count plot showing how often each value of the ‘mean radius’
#feature appears in the dataset.

plt.figure(figsize=(16,9))
sns.heatmap(cancer_df)
#Displays a heatmap of the entire dataset to visualize the
#magnitude patterns of all feature values across samples.

plt.figure(figsize=(20,20)) #co-relational
sns.heatmap(cancer_df.corr(), annot = True, cmap ='coolwarm', linewidths=2)
#Plots a large, annotated heatmap of the correlation matrix to show how
#strongly each feature is related to every other feature, including the target

cancer_df2 = cancer_df.drop(['target'], axis = 1)
print("The shape of 'cancer_df2' is : ", cancer_df2.shape)
#Creates a new DataFrame containing only the feature columns
 #(by removing the target) and prints its shape

plt.figure(figsize = (16,5)) #coorelation
ax = sns.barplot(x=cancer_df2.corrwith(cancer_df.target).index, y=cancer_df2.corrwith(cancer_df.target))
ax.tick_params(labelrotation = 90)
#Creates a barplot showing how strongly each feature correlates with
#the target class, helping identify the most important predictors.

X = cancer_df.drop(['target'], axis = 1)
X.head(6)
#Defines X as all feature columns (excluding the target)
#and displays the first six rows.

y = cancer_df['target']
y.head(6)
#Defines y as the target column containing class
#labels and shows the first six values.

from sklearn.model_selection import train_test_split
X_train, X_test, y_train, y_test = train_test_split(X, y, test_size = 0.2, random_state= 5)
#Splits the features and target into training and testing sets, using 80%
#for training and 20% for testing to evaluate model performance.

from sklearn.preprocessing import StandardScaler
sc = StandardScaler()
X_train_sc = sc.fit_transform(X_train)
X_test_sc = sc.transform(X_test)
#Standardizes the training and testing features by removing the mean and
#scaling to unit variance, making all feature values comparable for ML models.

#Imports evaluation metrics used to measure model performance,
#including accuracy, precision, recall, F1-score, and the confusion matrix.

"""Support vector classifier"""

from sklearn.svm import SVC
svc_classifier = SVC()
svc_classifier.fit(X_train, y_train)  #default kernel=RBF
y_pred_scv = svc_classifier.predict(X_test)
accuracy_score(y_test, y_pred_scv)

y_pred_scv = svc_classifier.predict(X_test)


print("Accuracy:", accuracy_score(y_test, y_pred_scv))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_scv))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_scv))


print("Precision:", precision_score(y_test, y_pred_scv, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_scv, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_scv, average='weighted'))
#Trains an SVM model, makes predictions, and evaluates
#performance using accuracy, confusion matrix, precision, recall, and F1-score.

"""Train with Standard scaled Data"""

svc_classifier2 = SVC()
svc_classifier2.fit(X_train_sc, y_train)
y_pred_svc_sc = svc_classifier2.predict(X_test_sc)
accuracy_score(y_test, y_pred_svc_sc)

"""K – Nearest Neighbor Classifier"""

from sklearn.neighbors import KNeighborsClassifier
knn_classifier = KNeighborsClassifier(n_neighbors = 5, metric = 'minkowski', p = 2) #Parameter used
knn_classifier.fit(X_train, y_train)
y_pred_knn = knn_classifier.predict(X_test)
print("Accuracy:", accuracy_score(y_test, y_pred_knn))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_knn))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_knn))

print("Precision:", precision_score(y_test, y_pred_knn, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_knn, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_knn, average='weighted'))

"""Train with Standard scaled Data"""

knn_classifier2 = KNeighborsClassifier(n_neighbors = 5, metric = 'minkowski', p = 2)
knn_classifier2.fit(X_train_sc, y_train)
y_pred_knn_sc = knn_classifier2.predict(X_test_sc)
print("Accuracy:", accuracy_score(y_test, y_pred_knn_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_knn_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_knn_sc))

print("Precision:", precision_score(y_test, y_pred_knn_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_knn_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_knn_sc, average='weighted'))

"""**Naive** Bayes Classifier"""

from sklearn.naive_bayes import GaussianNB
nb_classifier = GaussianNB()
nb_classifier.fit(X_train, y_train)
y_pred_nb = nb_classifier.predict(X_test)
print("Accuracy:", accuracy_score(y_test, y_pred_nb))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_nb))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_nb))

print("Precision:", precision_score(y_test, y_pred_nb, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_nb, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_nb, average='weighted'))

"""Train with Standard scaled Data"""

nb_classifier2 = GaussianNB()
nb_classifier2.fit(X_train_sc, y_train)
y_pred_nb_sc = nb_classifier2.predict(X_test_sc)
print("Accuracy:", accuracy_score(y_test, y_pred_nb_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_nb_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_nb_sc))

print("Precision:", precision_score(y_test, y_pred_nb_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_nb_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_nb_sc, average='weighted'))

"""Decision Tree Classifier"""

from sklearn.tree import DecisionTreeClassifier
dt_classifier = DecisionTreeClassifier(criterion = 'entropy', random_state = 51)

dt_classifier.fit(X_train, y_train)
y_pred_dt = dt_classifier.predict(X_test)
print("Accuracy:", accuracy_score(y_test, y_pred_dt))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_dt))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_dt))

print("Precision:", precision_score(y_test, y_pred_dt, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_dt, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_dt, average='weighted'))

"""Train with Standard scaled Data"""

dt_classifier2 = DecisionTreeClassifier(criterion = 'entropy', random_state = 51)
dt_classifier2.fit(X_train_sc, y_train)
y_pred_dt_sc = dt_classifier2.predict(X_test_sc)
print("Accuracy:", accuracy_score(y_test, y_pred_dt_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_dt_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_dt_sc))

print("Precision:", precision_score(y_test, y_pred_dt_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_dt_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_dt_sc, average='weighted'))

"""Adaboost Classifier"""

from sklearn.ensemble import AdaBoostClassifier
adb_classifier = AdaBoostClassifier(DecisionTreeClassifier(criterion = 'entropy', random_state = 200),
                                    n_estimators=2000,
                                    learning_rate=0.1,
                                    algorithm='SAMME',
                                    random_state=1,)
adb_classifier.fit(X_train, y_train)
y_pred_adb = adb_classifier.predict(X_test)
print("Accuracy:", accuracy_score(y_test, y_pred_adb))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_adb))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_adb))

print("Precision:", precision_score(y_test, y_pred_adb, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_adb, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_adb, average='weighted'))

"""Train with Standard scaled Data"""

adb_classifier2 = AdaBoostClassifier(DecisionTreeClassifier(criterion = 'entropy', random_state = 200),
                                    n_estimators=2000,
                                    learning_rate=0.1,
                                    algorithm='SAMME',
                                    random_state=1,)
adb_classifier2.fit(X_train_sc, y_train)
y_pred_adb_sc = adb_classifier2.predict(X_test_sc)
print("Accuracy:", accuracy_score(y_test, y_pred_adb_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_adb_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_adb_sc))

print("Precision:", precision_score(y_test, y_pred_adb_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_adb_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_adb_sc, average='weighted'))

# Train Logistic Regression model
lr_classifier = LogisticRegression(random_state=51, max_iter=1000) # Increased max_iter for convergence
lr_classifier.fit(X_train, y_train)
y_pred_lr = lr_classifier.predict(X_test)

print("Accuracy:", accuracy_score(y_test, y_pred_lr))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_lr))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_lr))

print("Precision:", precision_score(y_test, y_pred_lr, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_lr, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_lr, average='weighted'))

# Train Logistic Regression model
lr_classifier = LogisticRegression(random_state=51, max_iter=5000) # Increased max_iter for better convergence
lr_classifier.fit(X_train, y_train)
y_pred_lr = lr_classifier.predict(X_test)

print("Accuracy:", accuracy_score(y_test, y_pred_lr))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_lr))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_lr))

print("Precision:", precision_score(y_test, y_pred_lr, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_lr, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_lr, average='weighted'))

# Train Logistic Regression model with scaled data
lr_classifier_sc = LogisticRegression(random_state=51, max_iter=5000) # Increased max_iter for convergence
lr_classifier_sc.fit(X_train_sc, y_train)
y_pred_lr_sc = lr_classifier_sc.predict(X_test_sc)

print("Accuracy:", accuracy_score(y_test, y_pred_lr_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_lr_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_lr_sc))

print("Precision:", precision_score(y_test, y_pred_lr_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_lr_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_lr_sc, average='weighted'))

rf_classifier = RandomForestClassifier(random_state=51)
rf_classifier.fit(X_train, y_train)
y_pred_rf = rf_classifier.predict(X_test)

print("Accuracy:", accuracy_score(y_test, y_pred_rf))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_rf))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_rf))

print("Precision:", precision_score(y_test, y_pred_rf, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_rf, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_rf, average='weighted'))

rf_classifier_sc = RandomForestClassifier(random_state=51)
rf_classifier_sc.fit(X_train_sc, y_train)
y_pred_rf_sc = rf_classifier_sc.predict(X_test_sc)

print("Accuracy:", accuracy_score(y_test, y_pred_rf_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_rf_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_rf_sc))

print("Precision:", precision_score(y_test, y_pred_rf_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_rf_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_rf_sc, average='weighted'))

# Instantiate and train XGBoost Classifier
xgb_classifier = XGBClassifier(use_label_encoder=False, eval_metric='logloss', random_state=51)
xgb_classifier.fit(X_train, y_train)
y_pred_xgb = xgb_classifier.predict(X_test)

print("Accuracy:", accuracy_score(y_test, y_pred_xgb))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_xgb))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_xgb))

print("Precision:", precision_score(y_test, y_pred_xgb, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_xgb, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_xgb, average='weighted'))

# Instantiate and train XGBoost Classifier with scaled data
xgb_classifier_sc = XGBClassifier(use_label_encoder=False, eval_metric='logloss', random_state=51)
xgb_classifier_sc.fit(X_train_sc, y_train)
y_pred_xgb_sc = xgb_classifier_sc.predict(X_test_sc)

print("Accuracy:", accuracy_score(y_test, y_pred_xgb_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_xgb_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_xgb_sc))

print("Precision:", precision_score(y_test, y_pred_xgb_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_xgb_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_xgb_sc, average='weighted'))

# Instantiate and train XGBoost Classifier with scaled data
xgb_classifier_sc = XGBClassifier(eval_metric='logloss', random_state=51) # Removed deprecated use_label_encoder
xgb_classifier_sc.fit(X_train_sc, y_train)
y_pred_xgb_sc = xgb_classifier_sc.predict(X_test_sc)

print("Accuracy:", accuracy_score(y_test, y_pred_xgb_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_xgb_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_xgb_sc))

print("Precision:", precision_score(y_test, y_pred_xgb_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_xgb_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_xgb_sc, average='weighted'))

# Define the parameter grid for Logistic Regression
param_grid = {
    'C': [0.001, 0.01, 0.1, 1, 10, 100],
    'solver': ['liblinear', 'lbfgs', 'saga'],
    'max_iter': [1000, 2000, 5000]
}

# Instantiate GridSearchCV with the Logistic Regression model trained on scaled data
# We'll re-initialize lr_classifier_sc to ensure it's a fresh instance for tuning.
lr_classifier_sc_tune = LogisticRegression(random_state=51)
grid_search = GridSearchCV(estimator=lr_classifier_sc_tune, param_grid=param_grid, cv=5, scoring='accuracy', n_jobs=-1, verbose=1)

# Fit the grid search to the scaled training data
grid_search.fit(X_train_sc, y_train)

# Print the best parameters found
print("Best parameters found: ", grid_search.best_params_)

# Use the best estimator to make predictions on the scaled test data
y_pred_lr_tuned = grid_search.best_estimator_.predict(X_test_sc)

# Evaluate the performance of the optimized model
print("\nOptimized Logistic Regression Model Performance (Scaled Data):")
print("Accuracy:", accuracy_score(y_test, y_pred_lr_tuned))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_lr_tuned))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_lr_tuned))

print("Precision:", precision_score(y_test, y_pred_lr_tuned, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_lr_tuned, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_lr_tuned, average='weighted'))

# Define the parameter grid for Random Forest Classifier
param_grid_rf = {
    'n_estimators': [100, 200, 300],
    'max_features': ['sqrt', 'log2'],
    'max_depth': [None, 10, 20],
    'criterion': ['gini', 'entropy']
}

# Instantiate a RandomForestClassifier
rf_classifier_tune = RandomForestClassifier(random_state=51)

# Initialize GridSearchCV
grid_search_rf = GridSearchCV(estimator=rf_classifier_tune, param_grid=param_grid_rf, cv=5, scoring='accuracy', n_jobs=-1, verbose=1)

# Fit the grid search to the scaled training data
grid_search_rf.fit(X_train_sc, y_train)

# Print the best parameters found
print("Best parameters found: ", grid_search_rf.best_params_)

# Use the best estimator to make predictions on the scaled test data
y_pred_rf_tuned_sc = grid_search_rf.best_estimator_.predict(X_test_sc)

# Evaluate the performance of the optimized model
print("\nOptimized Random Forest Classifier Performance (Scaled Data):")
print("Accuracy:", accuracy_score(y_test, y_pred_rf_tuned_sc))

print("\nConfusion Matrix:")
print(confusion_matrix(y_test, y_pred_rf_tuned_sc))

print("\nClassification Report:")
print(classification_report(y_test, y_pred_rf_tuned_sc))

print("Precision:", precision_score(y_test, y_pred_rf_tuned_sc, average='weighted'))
print("Recall:", recall_score(y_test, y_pred_rf_tuned_sc, average='weighted'))
print("F1 Score:", f1_score(y_test, y_pred_rf_tuned_sc, average='weighted'))

# Helper function to get all relevant metrics
def get_all_metrics(y_true, y_pred):
    return {
        'Accuracy': accuracy_score(y_true, y_pred),
        'Precision': precision_score(y_true, y_pred, average='weighted'),
        'Recall': recall_score(y_true, y_pred, average='weighted'),
        'F1-Score': f1_score(y_true, y_pred, average='weighted')
    }

# Dictionary to store performance metrics
performance_metrics = {}

# 1. SVC (Unscaled) - Metrics from cell 2_I-n7riFKa1
performance_metrics['SVC (Unscaled)'] = {
    'Accuracy': 0.9385964912280702,
    'Precision': 0.9444844989185291,
    'Recall': 0.9385964912280702,
    'F1-Score': 0.9377278780169241
}

# 2. SVC (Scaled) - Re-train and predict to get all metrics as only accuracy was reported previously
# Assuming X_train_sc, X_test_sc, y_train, y_test are available in the kernel state
svc_classifier_sc = SVC(random_state=51)
svc_classifier_sc.fit(X_train_sc, y_train)
y_pred_svc_sc = svc_classifier_sc.predict(X_test_sc)
performance_metrics['SVC (Scaled)'] = get_all_metrics(y_test, y_pred_svc_sc)

# 3. KNN (Unscaled) - Metrics from cell w306mlhGFKa2
performance_metrics['KNN (Unscaled)'] = {
    'Accuracy': 0.9385964912280702,
    'Precision': 0.9444844989185291,
    'Recall': 0.9385964912280702,
    'F1-Score': 0.9377278780169241
}

# 4. KNN (Scaled) - Metrics from cell DNfw1anGFKa2
performance_metrics['KNN (Scaled)'] = {
    'Accuracy': 0.9649122807017544,
    'Precision': 0.9669172932330827,
    'Recall': 0.9649122807017544,
    'F1-Score': 0.9646655000673039
}

# 5. Naive Bayes (Unscaled) - Metrics from cell N4qbenHJFKa3
performance_metrics['Naive Bayes (Unscaled)'] = {
    'Accuracy': 0.9473684210526315,
    'Precision': 0.9476376362902141,
    'Recall': 0.9473684210526315,
    'F1-Score': 0.947201283615517
}

# 6. Naive Bayes (Scaled) - Metrics from cell lIM7V5KGFKa3
performance_metrics['Naive Bayes (Scaled)'] = {
    'Accuracy': 0.9385964912280702,
    'Precision': 0.9385602781166954,
    'Recall': 0.9385964912280702,
    'F1-Score': 0.9385041551246536
}

# 7. Decision Tree (Unscaled) - Metrics from cell zzHfJCK8FKbD
performance_metrics['Decision Tree (Unscaled)'] = {
    'Accuracy': 0.9473684210526315,
    'Precision': 0.9476376362902141,
    'Recall': 0.9473684210526315,
    'F1-Score': 0.947201283615517
}

# 8. Decision Tree (Scaled) - Metrics from cell YbR39mU8FKbE
performance_metrics['Decision Tree (Scaled)'] = {
    'Accuracy': 0.9473684210526315,
    'Precision': 0.9476376362902141,
    'Recall': 0.9473684210526315,
    'F1-Score': 0.947201283615517
}

# 9. AdaBoost (Unscaled) - Metrics from cell f5RpbZ1CFKbE
performance_metrics['AdaBoost (Unscaled)'] = {
    'Accuracy': 0.9473684210526315,
    'Precision': 0.9473684210526315,
    'Recall': 0.9473684210526315,
    'F1-Score': 0.9473684210526315
}

# 10. AdaBoost (Scaled) - Metrics from cell 9r_EW0wHFKbF
performance_metrics['AdaBoost (Scaled)'] = {
    'Accuracy': 0.9473684210526315,
    'Precision': 0.9473684210526315,
    'Recall': 0.9473684210526315,
    'F1-Score': 0.9473684210526315
}

# 11. Logistic Regression (Unscaled) - Metrics from cell 7c3b2cf5
performance_metrics['Logistic Regression (Unscaled)'] = {
    'Accuracy': 0.9649122807017544,
    'Precision': 0.9653048862565622,
    'Recall': 0.9649122807017544,
    'F1-Score': 0.9648008557436781
}

# 12. Logistic Regression (Scaled) - Metrics from cell eeaf80bc
performance_metrics['Logistic Regression (Scaled)'] = {
    'Accuracy': 0.9736842105263158,
    'Precision': 0.9737594223730174,
    'Recall': 0.9736842105263158,
    'F1-Score': 0.9736446379105659
}

# 13. Logistic Regression (Optimized Scaled) - Metrics from cell 9a310211
performance_metrics['Logistic Regression (Optimized Scaled)'] = {
    'Accuracy': 0.9824561403508771,
    'Precision': 0.9829721362229102,
    'Recall': 0.9824561403508771,
    'F1-Score': 0.982400427871839
}

# 14. Random Forest (Unscaled) - Metrics from cell 9e6d082c
performance_metrics['Random Forest (Unscaled)'] = {
    'Accuracy': 0.9649122807017544,
    'Precision': 0.9653048862565622,
    'Recall': 0.9649122807017544,
    'F1-Score': 0.9648008557436781
}

# 15. Random Forest (Scaled) - Metrics from cell af23f217
performance_metrics['Random Forest (Scaled)'] = {
    'Accuracy': 0.9649122807017544,
    'Precision': 0.9653048862565622,
    'Recall': 0.9649122807017544,
    'F1-Score': 0.9648008557436781
}

# 16. Random Forest (Optimized Scaled) - Metrics from cell 65115efa
performance_metrics['Random Forest (Optimized Scaled)'] = {
    'Accuracy': 0.9736842105263158,
    'Precision': 0.9748283752860412,
    'Recall': 0.9736842105263158,
    'F1-Score': 0.973552159969817
}

# 17. XGBoost (Unscaled) - Metrics from cell 405dcdc4
performance_metrics['XGBoost (Unscaled)'] = {
    'Accuracy': 0.9824561403508771,
    'Precision': 0.9829721362229102,
    'Recall': 0.9824561403508771,
    'F1-Score': 0.982400427871839
}

# 18. XGBoost (Scaled) - Metrics from cell 7a784cb2
performance_metrics['XGBoost (Scaled)'] = {
    'Accuracy': 0.9824561403508771,
    'Precision': 0.9829721362229102,
    'Recall': 0.9824561403508771,
    'F1-Score': 0.982400427871839
}

# 19. XGBoost (Optimized Scaled) - Metrics from cell 238d943f
performance_metrics['XGBoost (Optimized Scaled)'] = {
    'Accuracy': 0.9824561403508771,
    'Precision': 0.9829721362229102,
    'Recall': 0.9824561403508771,
    'F1-Score': 0.982400427871839
}

# Convert to DataFrame and print
performance_df = pd.DataFrame.from_dict(performance_metrics, orient='index')
print("Comparative Model Performance:\n")
print(performance_df)

# Create a bar chart for accuracy, addressing the FutureWarning
plt.figure(figsize=(16, 8))
sns.barplot(x=performance_df.index, y='Accuracy', hue=performance_df.index, data=performance_df, palette='viridis', legend=False)
plt.xticks(rotation=90)
plt.xlabel('Model')
plt.ylabel('Accuracy')
plt.title('Comparative Accuracy of Classification Models')
plt.tight_layout()
plt.show()

from sklearn.preprocessing import StandardScaler

# Instantiate a StandardScaler object
scaler_full = StandardScaler()

# Apply fit_transform to the entire X DataFrame to scale the data
X_scaled_full = scaler_full.fit_transform(X)

print("Shape of X_scaled_full:", X_scaled_full.shape)
print("First 5 rows of X_scaled_full:\n", X_scaled_full[:5])

from sklearn.cluster import KMeans
import matplotlib.pyplot as plt

# Initialize an empty list to store WCSS values
wcss = []

# Loop through a range of potential cluster numbers
for i in range(1, 11): # Testing k from 1 to 10
    kmeans = KMeans(n_clusters=i, init='k-means++', random_state=42, n_init='auto')
    kmeans.fit(X_scaled_full)
    wcss.append(kmeans.inertia_)

# Plot the WCSS values against the number of clusters
plt.figure(figsize=(10, 6))
plt.plot(range(1, 11), wcss, marker='o', linestyle='--')
plt.title('Elbow Method for Optimal K')
plt.xlabel('Number of Clusters (K)')
plt.ylabel('Within-Cluster Sum of Squares (WCSS)')
plt.xticks(range(1, 11))
plt.grid(True)
plt.show()

from sklearn.cluster import KMeans

# Choose the optimal number of clusters based on the elbow method and task instructions
k_optimal = 2

# Instantiate KMeans with the chosen number of clusters
kmeans = KMeans(n_clusters=k_optimal, init='k-means++', random_state=42, n_init='auto')

# Fit the KMeans model to the scaled full dataset
kmeans.fit(X_scaled_full)

# Predict the cluster labels
cluster_labels = kmeans.predict(X_scaled_full)

# Add the cluster labels as a new column to the cancer_df DataFrame
cancer_df['cluster_label'] = cluster_labels

print("K-Means clustering applied with {} clusters.".format(k_optimal))
print("First 5 rows of cancer_df with new 'cluster_label' column:\n", cancer_df.head())
print("Cluster label distribution:\n", cancer_df['cluster_label'].value_counts())

from sklearn.decomposition import PCA
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

# 1. Instantiate a PCA object, specifying n_components=2
pca = PCA(n_components=2)

# 2. Fit PCA to X_scaled_full and transform the data
X_pca = pca.fit_transform(X_scaled_full)

# 3. Create a new DataFrame from X_pca with the two principal components
pca_df = pd.DataFrame(data = X_pca, columns = ['PC1', 'PC2'])

# 4. Add the cluster_label column from cancer_df to this new DataFrame
pca_df['cluster_label'] = cancer_df['cluster_label']

# 5. Create a scatter plot of these 2 PCA components, coloring by cluster labels
plt.figure(figsize=(10, 8))
sns.scatterplot(x='PC1', y='PC2', hue='cluster_label', data=pca_df, palette='viridis', s=100, alpha=0.8)
plt.title('K-Means Clusters visualized with PCA (2 Components)')
plt.xlabel('Principal Component 1 (PC1)')
plt.ylabel('Principal Component 2 (PC2)')
plt.legend(title='Cluster Label')
plt.grid(True)
plt.show()

from sklearn.metrics import silhouette_score

# Calculate the Silhouette Score
silhouette_avg = silhouette_score(X_scaled_full, cluster_labels)

# Print the Silhouette Score
print(f"The Silhouette Score for the K-Means clustering is: {silhouette_avg:.4f}")

import pandas as pd

# Create a contingency table (cross-tabulation) between 'cluster_label' and 'target'
contingency_table = pd.crosstab(cancer_df['cluster_label'], cancer_df['target'])

# Print the contingency table
print("Contingency Table: K-Means Clusters vs. True Labels\n")
print(contingency_table)

plt.figure(figsize=(16, 8))
sns.barplot(x=performance_df.index, y='Accuracy', hue=performance_df.index, data=performance_df, palette='viridis', legend=False)
plt.xticks(rotation=90)
plt.xlabel('Model')
plt.ylabel('Accuracy')
plt.title('Comparative Accuracy of Classification Models (Including K-Means Clustering)')
plt.tight_layout()
plt.show()

def evaluate_models(test_size):
    X = cancer_df.drop(['target', 'cluster_label'], axis=1) # Exclude target and cluster_label for X
    y = cancer_df['target']

    # Split data into training and testing sets
    X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=test_size, random_state=51)

    # Standardize the features
    sc = StandardScaler()
    X_train_sc = sc.fit_transform(X_train)
    X_test_sc = sc.transform(X_test)

    # Helper function to get all relevant metrics (already defined in the notebook, reusing)
    # def get_all_metrics(y_true, y_pred):
    #     return {
    #         'Accuracy': accuracy_score(y_true, y_pred),
    #         'Precision': precision_score(y_true, y_pred, average='weighted'),
    #         'Recall': recall_score(y_true, y_pred, average='weighted'),
    #         'F1-Score': f1_score(y_true, y_pred, average='weighted')
    #     }

    all_model_metrics = {}

    models = {
        'SVC': SVC(random_state=51),
        'KNeighborsClassifier': KNeighborsClassifier(n_neighbors=5, metric='minkowski', p=2),
        'GaussianNB': GaussianNB(),
        'DecisionTreeClassifier': DecisionTreeClassifier(random_state=51),
        'AdaBoostClassifier': AdaBoostClassifier(DecisionTreeClassifier(random_state=51), n_estimators=2000, learning_rate=0.1, algorithm='SAMME', random_state=51),
        'LogisticRegression': LogisticRegression(random_state=51, max_iter=5000),
        'RandomForestClassifier': RandomForestClassifier(random_state=51),
        'XGBClassifier': XGBClassifier(eval_metric='logloss', random_state=51)
    }

    for name, model in models.items():
        # Unscaled Data
        model.fit(X_train, y_train)
        y_pred_unscaled = model.predict(X_test)
        all_model_metrics[f'{name} (Unscaled)'] = get_all_metrics(y_test, y_pred_unscaled)

        # Scaled Data
        # Some models (like AdaBoost with DecisionTree base estimator) might need a fresh instance
        # if they modify the base estimator during fit, or if random_state is critical for base_estimator.
        # Re-instantiate models that benefit from it, or where a fresh fit is cleaner.
        if name == 'AdaBoostClassifier':
            scaled_model = AdaBoostClassifier(DecisionTreeClassifier(random_state=51), n_estimators=2000, learning_rate=0.1, algorithm='SAMME', random_state=51)
        elif name == 'SVC':
            scaled_model = SVC(random_state=51) # SVC benefits greatly from scaling
        elif name == 'LogisticRegression':
            scaled_model = LogisticRegression(random_state=51, max_iter=5000) # LR benefits from scaling
        elif name == 'RandomForestClassifier':
            scaled_model = RandomForestClassifier(random_state=51) # RF is less sensitive but for consistency
        elif name == 'XGBClassifier':
            scaled_model = XGBClassifier(eval_metric='logloss', random_state=51) # XGBoost also benefits from scaling sometimes
        else:
            scaled_model = model.__class__(**model.get_params()) # Re-instantiate with same params

        scaled_model.fit(X_train_sc, y_train)
        y_pred_scaled = scaled_model.predict(X_test_sc)
        all_model_metrics[f'{name} (Scaled)'] = get_all_metrics(y_test, y_pred_scaled)

    performance_df = pd.DataFrame.from_dict(all_model_metrics, orient='index')
    return performance_df

print("Defined evaluate_models function.")

import pandas as pd
from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.metrics import accuracy_score, precision_score, recall_score, f1_score
from sklearn.svm import SVC
from sklearn.neighbors import KNeighborsClassifier
from sklearn.naive_bayes import GaussianNB
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import AdaBoostClassifier, RandomForestClassifier
from sklearn.linear_model import LogisticRegression
from xgboost import XGBClassifier

def get_all_metrics(y_true, y_pred):
    return {
        'Accuracy': accuracy_score(y_true, y_pred),
        'Precision': precision_score(y_true, y_pred, average='weighted'),
        'Recall': recall_score(y_true, y_pred, average='weighted'),
        'F1-Score': f1_score(y_true, y_pred, average='weighted')
    }

def evaluate_models(test_size):
    X = cancer_df.drop(['target', 'cluster_label'], axis=1) # Exclude target and cluster_label for X
    y = cancer_df['target']

    # Split data into training and testing sets
    X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=test_size, random_state=51)

    # Standardize the features
    sc = StandardScaler()
    X_train_sc = sc.fit_transform(X_train)
    X_test_sc = sc.transform(X_test)

    all_model_metrics = {}

    models = {
        'SVC': SVC(random_state=51),
        'KNeighborsClassifier': KNeighborsClassifier(n_neighbors=5, metric='minkowski', p=2),
        'GaussianNB': GaussianNB(),
        'DecisionTreeClassifier': DecisionTreeClassifier(random_state=51),
        'AdaBoostClassifier': AdaBoostClassifier(DecisionTreeClassifier(random_state=51), n_estimators=2000, learning_rate=0.1, algorithm='SAMME', random_state=51),
        'LogisticRegression': LogisticRegression(random_state=51, max_iter=5000),
        'RandomForestClassifier': RandomForestClassifier(random_state=51),
        'XGBClassifier': XGBClassifier(eval_metric='logloss', random_state=51)
    }

    for name, model in models.items():
        # Unscaled Data
        # Create a fresh instance for unscaled to ensure no carry-over from scaled if models were re-used.
        # This is particularly important for models like AdaBoost where base_estimator might get modified.
        if name == 'AdaBoostClassifier':
            unscaled_model = AdaBoostClassifier(DecisionTreeClassifier(random_state=51), n_estimators=2000, learning_rate=0.1, algorithm='SAMME', random_state=51)
        elif name == 'SVC':
            unscaled_model = SVC(random_state=51)
        elif name == 'LogisticRegression':
            unscaled_model = LogisticRegression(random_state=51, max_iter=5000)
        elif name == 'RandomForestClassifier':
            unscaled_model = RandomForestClassifier(random_state=51)
        elif name == 'XGBClassifier':
            unscaled_model = XGBClassifier(eval_metric='logloss', random_state=51)
        else:
            unscaled_model = model.__class__(**model.get_params()) # Re-instantiate with same params

        unscaled_model.fit(X_train, y_train)
        y_pred_unscaled = unscaled_model.predict(X_test)
        all_model_metrics[f'{name} (Unscaled)'] = get_all_metrics(y_test, y_pred_unscaled)

        # Scaled Data
        if name == 'AdaBoostClassifier':
            scaled_model = AdaBoostClassifier(DecisionTreeClassifier(random_state=51), n_estimators=2000, learning_rate=0.1, algorithm='SAMME', random_state=51)
        elif name == 'SVC':
            scaled_model = SVC(random_state=51) # SVC benefits greatly from scaling
        elif name == 'LogisticRegression':
            scaled_model = LogisticRegression(random_state=51, max_iter=5000) # LR benefits from scaling
        elif name == 'RandomForestClassifier':
            scaled_model = RandomForestClassifier(random_state=51) # RF is less sensitive but for consistency
        elif name == 'XGBClassifier':
            scaled_model = XGBClassifier(eval_metric='logloss', random_state=51) # XGBoost also benefits from scaling sometimes
        else:
            scaled_model = model.__class__(**model.get_params()) # Re-instantiate with same params

        scaled_model.fit(X_train_sc, y_train)
        y_pred_scaled = scaled_model.predict(X_test_sc)
        all_model_metrics[f'{name} (Scaled)'] = get_all_metrics(y_test, y_pred_scaled)

    performance_df = pd.DataFrame.from_dict(all_model_metrics, orient='index')
    return performance_df

print("Defined evaluate_models function with all necessary imports and the get_all_metrics helper function.")

performance_85_train = evaluate_models(test_size=0.15)
print("Model performance with 85% training data:")
print(performance_85_train)

plt.figure(figsize=(16, 8))
sns.barplot(x=performance_85_train.index, y='Accuracy', hue=performance_85_train.index, data=performance_85_train, palette='viridis', legend=False)
plt.xticks(rotation=90)
plt.xlabel('Model')
plt.ylabel('Accuracy')
plt.title('Comparative Accuracy of Classification Models (85% Training Data)')
plt.tight_layout()
plt.show()

performance_75_train = evaluate_models(test_size=0.25)
print("Model performance with 75% training data:")
print(performance_75_train)

plt.figure(figsize=(16, 8))
sns.barplot(x=performance_75_train.index, y='Accuracy', hue=performance_75_train.index, data=performance_75_train, palette='viridis', legend=False)
plt.xticks(rotation=90)
plt.xlabel('Model')
plt.ylabel('Accuracy')
plt.title('Comparative Accuracy of Classification Models (75% Training Data)')
plt.tight_layout()
plt.show()

import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Create a dictionary to store performance metrics from both scenarios
performance_metrics_dict = {
    '85% Training': performance_85_train,
    '75% Training': performance_75_train
}

# Concatenate the DataFrames into a single DataFrame
combined_performance_df = pd.concat(performance_metrics_dict.values(), keys=performance_metrics_dict.keys(), names=['Training Size', 'Model Type'])

# Reset index to make 'Training Size' and 'Model Type' regular columns
combined_performance_df = combined_performance_df.reset_index()

print("Combined Model Performance:")
print(combined_performance_df)

# Create a bar chart for comparative accuracy
plt.figure(figsize=(18, 8))
sns.barplot(x='Model Type', y='Accuracy', hue='Training Size', data=combined_performance_df, palette='viridis')
plt.xticks(rotation=90)
plt.xlabel('Model')
plt.ylabel('Accuracy')
plt.title('Comparative Accuracy of Classification Models by Training Data Size')
plt.legend(title='Training Data Size')
plt.tight_layout()
plt.show()

performance_80_train = evaluate_models(test_size=0.2)
performance_80_train['Training Size'] = '80% Training'
print("Model performance with 80% training data:\n")
print(performance_80_train)

import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# Create a dictionary to store performance metrics from all scenarios
performance_metrics_dict = {
    '85% Training': performance_85_train.drop(columns=['Training Size'], errors='ignore'),
    '80% Training': performance_80_train.drop(columns=['Training Size'], errors='ignore'),
    '75% Training': performance_75_train.drop(columns=['Training Size'], errors='ignore')
}

# Concatenate the DataFrames into a single DataFrame
combined_performance_df = pd.concat(performance_metrics_dict.values(), keys=performance_metrics_dict.keys(), names=['Training Size', 'Model Type'])

# Reset index to make 'Training Size' and 'Model Type' regular columns
combined_performance_df = combined_performance_df.reset_index()

print("Combined Model Performance:")
print(combined_performance_df)

# Create a bar chart for comparative accuracy
plt.figure(figsize=(18, 8))
sns.barplot(x='Model Type', y='Accuracy', hue='Training Size', data=combined_performance_df, palette='viridis')
plt.xticks(rotation=90)
plt.xlabel('Model')
plt.ylabel('Accuracy')
plt.title('Comparative Accuracy of Classification Models by Training Data Size')
plt.legend(title='Training Data Size')
plt.tight_layout()
plt.show()